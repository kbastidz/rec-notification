
spring.application.name=notification-service
server.port=8081
# Agregar configuración adicional
spring.profiles.active=dev
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# DataSource
spring.datasource.url=jdbc:postgresql://localhost:5432/notifications_db
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kevinbastidas784@gmail.com
spring.mail.password=afne zmks kgva jruv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Multipart
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Firebase
#firebase.config-path=classpath:firebase-service-account.json
firebase.config-path=firebase-service-account.json

# Notification
notification.from-email=kevinbastidas784@gmail.com
notification.from-name=Notification Service

# Logging
logging.level.com.notification=DEBUG
